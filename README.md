# FinOps Wizard

FinOps Wizard is a local-first, trust-building mock FinOps discovery, inventory, analysis, local dashboard (TUI), and alerting CLI.

## Installation
Run with `uv`:
```bash
uv run finops-wizard
```
